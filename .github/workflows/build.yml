name: Build

on:
  pull_request:
  push:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build'
        required: false
        default: 'master'

jobs:
  build:
    name: Build Firmware
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.inputs.branch || github.ref }}
          submodules: recursive
      
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v14

      - name: Cache Nix store
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/nix
            /nix/store
          key: nix-${{ runner.os }}-${{ hashFiles('flake.lock') }}
          restore-keys: |
            nix-${{ runner.os }}-

      - name: Build Unix simulator
        run: nix develop -c make unix

      - name: Build release binaries (ST-Link + drag-and-drop)
        run: nix develop -c make release-binaries

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-binaries
          path: |
            bin/specter-diy.bin
            bin/specter-diy.hex

      - name: Upload ST-Link binary
        uses: actions/upload-artifact@v4
        with:
          name: stlink-binary
          path: release/initial_firmware.bin

      - name: Upload drag-and-drop binary
        uses: actions/upload-artifact@v4
        with:
          name: drag-and-drop-binary
          path: release/specter_upgrade.bin
